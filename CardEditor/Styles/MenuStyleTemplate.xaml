<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="CustomMenuItemStyle" TargetType="{x:Type MenuItem}">
        <Setter Property="Background" Value="{Binding Background, ElementName=menudatamain}" />
        <Setter Property="Foreground" Value="{Binding Foreground, ElementName=menudatamain}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                <ColumnDefinition Width="13"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="Icon" Margin="6,0,6,0" VerticalAlignment="Center"
                                            ContentSource="Icon"/>
                            <ContentPresenter x:Name="HeaderHost" Grid.Column="1" 
                                            ContentSource="Header" RecognizesAccessKey="True"
                                            VerticalAlignment="Center"/>
                            <ContentPresenter x:Name="SubMenuItemsHost" Grid.Column="2" 
                                            Margin="0,0,0,0" VerticalAlignment="Center"
                                            ContentSource="InputGestureText"/>
                            <Path x:Name="Arrow" Grid.Column="3" Fill="{TemplateBinding Foreground}"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Data="M0,0 L0,8 L4,4 Z"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Placement="Right"
                                   HorizontalOffset="-4" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Focusable="false" PopupAnimation="Fade">
                                <Border x:Name="SubMenuBorder" Background="{Binding Background, ElementName=menudatamain}"
                                        BorderBrush="{Binding BorderBrush, ElementName=menudatamain}"
                                        BorderThickness="1">
                                    <ScrollViewer CanContentScroll="true" Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                        <ItemsPresenter Margin="2"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="true">
                            <Setter Property="Background" TargetName="Border" Value="{Binding Background, ElementName=menudatamain}"/>
                            <Setter Property="BorderBrush" TargetName="Border" Value="{Binding BorderBrush, ElementName=menudatamain}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="false">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="true">
                            <Setter TargetName="Arrow" Property="Fill" Value="{Binding Foreground, ElementName=menudatamain}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
<UserControl x:Class="ScriptSupport.Legacy.UserControls.CardInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:Behaviors="clr-namespace:ScriptSupport.Legacy.Behaviors"
             xmlns:Converter="clr-namespace:ScriptSupport.Legacy.Converter"
             xmlns:ViewModels="clr-namespace:ScriptSupport.Legacy.ViewModels"
             xmlns:Lag="clr-namespace:ScriptSupport.Legacy.Localization"
             xmlns:local="clr-namespace:ScriptSupport.Legacy.UserControls"
             mc:Ignorable="d" 
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Medium"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Stretch"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Background="{Binding Background, Source={x:Static ViewModels:UIConfigViewModel.Instance}}"
             Foreground="{Binding Foreground, Source={x:Static ViewModels:UIConfigViewModel.Instance}}"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             UseLayoutRounding="False"
             SnapsToDevicePixels="False"
             Unloaded="UserControl_Unloaded"
             d:DesignHeight="800" d:DesignWidth="400" >
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <materialDesign:Card Margin="0,1,0,0" Background="Transparent"
                             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                             FlowDirection="{Binding FlowDirectionC, Source={x:Static ViewModels:UIConfigViewModel.Instance}}"
                             d:FlowDirection="LeftToRight" >
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" >
                    <TextBlock Text="{Lag:Localize CardInfo}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                               Foreground="{Binding Foreground, Source={x:Static ViewModels:UIConfigViewModel.Instance}}"
                               FontFamily="{Binding FontFamily , Source={x:Static ViewModels:UIConfigViewModel.Instance}}"
                               FontSize="16" />
                </Grid>
                <Grid Grid.Row="1" >
                    <Image HorizontalAlignment="Stretch" Height="auto" Stretch="UniformToFill" Margin="7,3,7,0" />
                </Grid>

                <Grid Grid.Row="2" Margin="10,7,0,0" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" x:Name="grsetcode" >
                        <TextBlock x:Name="SetCodeLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <TextBlock x:Name="tblsetcode" TextWrapping="Wrap" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                    </WrapPanel>

                    <WrapPanel Grid.Row="1" x:Name="grcardtype" >
                        <TextBlock x:Name="CardTypeLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <TextBlock x:Name="tblcardtype" TextWrapping="Wrap" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                    </WrapPanel>

                    <WrapPanel Grid.Row="2" x:Name="grattribute" >
                        <TextBlock x:Name="CardAttriLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <TextBlock x:Name="tblmonsattri" TextWrapping="Wrap" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <Image x:Name="imgattri" Width="15" Height="15" Visibility="Collapsed" />
                    </WrapPanel>

                    <WrapPanel Grid.Row="3" x:Name="grrace" >
                        <TextBlock x:Name="CardRaceLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <TextBlock x:Name="tblmonsrace" TextWrapping="Wrap" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <Image x:Name="imgrace" Width="15" Height="15" Visibility="Collapsed" />
                    </WrapPanel>

                    <StackPanel Grid.Row="4" x:Name="grpendulum" Orientation="Horizontal" >
                        <TextBlock x:Name="PenScaleLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <TextBlock x:Name="tblPenLeft" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <Image x:Name="imgpenleft" Source="pack://application:,,,/Images/DataEditor/leftscale.ico" Width="15" Height="15" />
                        <TextBlock x:Name="tblPenRight" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <Image x:Name="imgpenright" Source="pack://application:,,,/Images/DataEditor/rightscale.ico" Width="15" Height="15" />
                    </StackPanel>
                    
                    <WrapPanel Grid.Row="5" x:Name="grlvrk" >
                        <TextBlock x:Name="LvRkLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <TextBlock x:Name="LvRkValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <Image x:Name="imgLvRk" Width="15" Height="15" />
                    </WrapPanel>
                    
                    <StackPanel Grid.Row="6" x:Name="grlink" Orientation="Vertical" >
                        <WrapPanel>
                            <TextBlock x:Name="LinkRatLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                            <TextBlock x:Name="LinkRatValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        </WrapPanel>
                        <WrapPanel>
                            <TextBlock x:Name="LinkArrLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                            <TextBlock x:Name="LinkArrValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                            <Image x:Name="imgLink" Source="pack://application:,,,/Images/DataEditor/linkIcon.ico" Width="15" Height="15" />
                        </WrapPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="7" x:Name="grmonspow" Orientation="Vertical" >
                        <StackPanel x:Name="gratk" Orientation="Horizontal" >
                            <TextBlock x:Name="AtkLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                            <TextBlock x:Name="AtkValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        </StackPanel>

                        <StackPanel x:Name="grdef" Orientation="Horizontal" >
                            <TextBlock x:Name="DefLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                            <TextBlock x:Name="DefValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        </StackPanel>

                        <StackPanel x:Name="grmaxatk" Orientation="Horizontal" Visibility="Collapsed" >
                            <TextBlock x:Name="MaxAtkLabel" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                            <TextBlock x:Name="MaxAtkValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        </StackPanel>
                    </StackPanel>

                    <WrapPanel Grid.Row="8" >
                        <TextBlock x:Name="IDValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                        <TextBlock x:Name="KonamiIDValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                    </WrapPanel>

                    <WrapPanel Grid.Row="8">
                        <TextBlock x:Name="RuleValue" Style="{StaticResource MaterialDesignHelperTextBlock}" />
                    </WrapPanel>
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>

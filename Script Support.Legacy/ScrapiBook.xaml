<Window x:Class="ScriptSupport.Legacy.ScrapiBook"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:local="clr-namespace:ScriptSupport.Legacy"
        xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.FontWeight="Medium"
        Style="{StaticResource MaterialDesignWindow}"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Loaded="Window_Loaded"
        Title="scrapibook" Height="450" Width="600">
    <Window.InputBindings>
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveCommand}" />
    </Window.InputBindings>
    <Grid x:Name="maingrid" Background="Transparent" Margin="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" Name="grleft"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="*" Name="grright"/>
        </Grid.ColumnDefinitions>

        <TreeView x:Name="FileTreeView" Grid.Column="0" Background="Transparent"
                  Style="{StaticResource MaterialDesignTreeView}"
                  SelectedItemChanged="FileTreeView_SelectedItemChanged" >

        </TreeView>
        <Border Grid.Column="1" Cursor="SizeWE" x:Name="separatorvertical"
                MouseMove="separatorvertical_MouseMove"
                MouseDown="separatorvertical_MouseDown"
                MouseUp="separatorvertical_MouseUp" />
        <avalonEdit:TextEditor Name="FileTextEditor" Grid.Column="2" Background="Transparent"
                               VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ShowLineNumbers="True" />
    </Grid>

</Window>

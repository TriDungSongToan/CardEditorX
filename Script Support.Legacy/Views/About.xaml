<Window x:Class="ScriptSupport.Legacy.Views.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScriptSupport.Legacy"
        xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.FontWeight="Medium"
        Style="{StaticResource MaterialDesignWindow}"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Title="About" Height="170" Width="320">
    <Grid>
        <materialDesign:Card x:Name="maingrid" Style="{StaticResource MaterialDesignOutlinedCard}" BorderThickness="1" BorderBrush="Black" >
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,10,10,5" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Grid.Row="0" Width="100" Height="100" >
                    <Image x:Name="AboutImage" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                </StackPanel>
                <StackPanel Grid.Column="2" Grid.Row="0" VerticalAlignment="Center" Margin="10">
                    <TextBlock Text="Script Support" FontSize="16" Margin="0,0,0,5"/>
                    <TextBlock Text="Version: BURH" FontSize="16" Margin="0,0,0,5"/>
                    <TextBlock Text="Data Source: that-hatter" FontSize="16" Margin="0,0,0,5"/>
                    <TextBlock Text="Created By: TriDung" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Margin="0,5,0,0">
                    <Button Name="btnOK" HorizontalAlignment="Center" IsDefault="True" Content="OK" Width="100" Height="30" Click="Button_Click" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
